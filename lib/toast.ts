/**
 * Re-export toast from sonner for app-wide notifications.
 * Use from any client component: import { toast } from '@/lib/toast';
 *
 * Examples:
 *   toast.success('Saved!');
 *   toast.error('Something went wrong');
 *   toast.info('Coming soon');
 */
export { toast } from 'sonner';
